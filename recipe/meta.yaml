{% set name = "zaber-motion" %}
{% set version = "2.14.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://gitlab.com/zaber-core-libs/zaber-motion-lib/-/archive/v{{ version }}/zaber-motion-lib-v{{ version }}.tar.gz
  sha256: b4d7b459a7f4d85aef53e60039fea037ac1c3b75e61149e6ea016d96dff66c5c
  folder: src/zaber-motion
  patches:
    - patches/no_bindings_install.patch
    - patches/dont_specify_python_3_in_executable_name.patch
    - patches/mypy_protobuf_2.patch

build:
  number: 1

requirements:
  build:
    - {{ compiler('cgo') }}
    - {{ compiler('m2w64_c') }}    # [win]
    - {{ compiler('m2w64_cxx') }}  # [win]
    - {{ compiler('c') }}          # [not win]
    - {{ compiler('cxx') }}        # [not win]
    - protoc-gen-go
    - python                                 # [build_platform != target_platform]
    - cross-python_{{ target_platform }}     # [build_platform != target_platform]
    - libprotobuf                            # [build_platform != target_platform]
  host:
    - python
    - pip
    - libprotobuf
    - protobuf
    - mypy-protobuf
  run:
    - python
    - protobuf
    - rx
    - __osx >={{ MACOSX_DEPLOYMENT_TARGET|default("10.9") }}  # [osx and x86_64]

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - zaber_motion

about:
  home: https://gitlab.com/zaber-core-libs/zaber-motion-lib
  license: MIT
  license_family: MIT
  license_file:
    - src/zaber-motion/LICENSE
    - LICENSE_GO_MODULES
  summary: A Python library used to operate Zaber devices.
  doc_url: https://www.zaber.com/software/docs/motion-library/
  dev_url: https://gitlab.com/zaber-core-libs/zaber-motion-lib

extra:
  recipe-maintainers:
    - hmaarrfk
